[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "oaei-bioml-eval"
version = "0.1.0.dev0"
description = "Common evaluation/scoring logic for the OAEI Bio-ML tracks (public + private)."
requires-python = ">=3.12"
# Planned together for the implemented release: version 0.2.0 and Python >=3.10.
# Do not change either early; the full Java-free gate is specs/0.2.0-native-owl-stack.md.
license = { text = "Apache-2.0" }
# Kept deliberately light so participants can self-score with a small install.
# The typed-ranking metrics (Track 2) and the structural-coherence proxy are
# dependency-free; the equivalence half (Track 1) adds DeepOnto when it lands.
dependencies = []

[project.optional-dependencies]
# Track 1 global submissions may arrive as OAEI Alignment RDF; rdflib parses them.
# (The metric core + the DeepOnto-TSV path stay dependency-free.)
rdf = ["rdflib>=7.0"]
# the official reasoner-based coherence, DEFAULT ROBOT backend: rdflib
# parses ROBOT's merged/unsat OWL output; ROBOT itself is an external Java tool located
# via PATH/$ROBOT_JAR (NOT pip — keeps JVM/torch out of eval's deps). pin ROBOT v1.9.10
# for released numbers (unsat counts are reasoner-version-dependent).
reasoner = ["rdflib>=7.0"]
# the OPTIONAL in-process warm-JVM coherence fast-path + the equivalence
# half's DeepOnto-TSV path. exploratory only — older HermiT/ELK than ROBOT.
deeponto = ["deeponto", "rdflib>=7.0"]

[project.scripts]
# self-score a Track 2 typed submission from the shell (see typed/cli.py)
oaei-bioml-typed-score = "oaei_bioml_eval.typed.cli:main"
# self-score a Track 1 equivalence submission (see equivalence/cli.py)
oaei-bioml-equivalence-score = "oaei_bioml_eval.equivalence.cli:main"
# organiser-computed Track 1 alignment coherence (see coherence/cli.py)
oaei-bioml-coherence-score = "oaei_bioml_eval.coherence.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/oaei_bioml_eval"]
